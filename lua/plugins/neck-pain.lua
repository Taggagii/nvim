return {
	"shortcuts/no-neck-pain.nvim",
	event = "VeryLazy", -- this lazy loads the plugin on a specific event
	opts = {
		autocmd = true, -- automatically enable on startup
		width = 150,
		-- add other NoNeckPain options here if desired
	}
}
